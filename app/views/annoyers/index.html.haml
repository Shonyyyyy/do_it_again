%h2 Annoyer Overview and Dashboard
%hr
.row
  .col-md-6
    %h3 Your Annoyers:
    - @annoyers_data.each do |annoyer_data|
      .annoyers-overview{ :style => "border-left-color:##{annoyer_data[:annoyer].color};" }
        %h4.list-group-item-heading= annoyer_data[:annoyer].title
        .label-annoyer
          = "Nodes (#{annoyer_data[:node_count]})"
        .label-annoyer
          - if annoyer_data[:date]
            = "latest recent: #{annoyer_data[:date]} (#{annoyer_data[:reminder]})"
          - else
            = "You never did that..."
        .clearfix
          = link_to "Manage", annoyer_path(annoyer_data[:annoyer]), :class => "btn btn-primary btn-sm pull-right"
  .col-md-6
    %h3 Your 10 latest Recents:
    %ul
      - @latest_recents.each do |recent|
        %li.list-group-item.list-group-item-recent{ :style => "background-color:##{recent[:color]}" }
          %strong= "#{recent[:reminder]} on "
          %i= recent[:date]
