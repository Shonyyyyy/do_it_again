%h2 Annoyer Overview and Dashboard
%hr
%div{ :class => "row" }
  %div{ :class => "col-md-6"}
    %h3 Your Annoyers:
    - @annoyers.each do |annoyer|
      %p
        %div{ :class => 'clearfix' }
          %div{ :class => 'list-group-item', :style => "background-color:##{annoyer.color};opacity: 0.6;" }
            %h4{ :class => 'list-groip-item-headin', :style => "color:#fff;" }= annoyer.title
